[package]
name = "termtint"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
csscolorparser = { version = "0.7", features = ["named-colors"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
